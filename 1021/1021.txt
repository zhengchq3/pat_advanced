1021 最深的根节点

一个连接的无环图可以被认为是一棵树。树的高度取决于选择的根节点。
你要找到使得树最高的根节点。这样的根节点叫做最深的根节点。

输入：
每个输入文件包含一个测试用例。
对于每个测试用例，第一行包含一个正整数N(<=10000)，代表节点数目。
节点标号是从1到N。
接下来是N-1行，每行描述了一条边，给出了两个相邻的节点的标号。

输出：
对于每个测试用例，在一行中输出每个最深根节点。
如果这样的根节点不唯一，以递增的顺序打印这些节点的标号。
如果给定的图不是一棵树，输出"Error: K componets"，K是在图中连接的组件的数目。
