电梯

我们城市的最高建筑只有一部电梯。由N个正数组成了一个请求列表。
数字表示电梯会以指定的顺序在哪一层停留。
电梯向上移动一层花费6秒，向下一层花费4秒。
电梯每次停留5秒。

给定一个请求列表，你要计算完成列表中的请求所花费的总时间。
电梯开始时在第0层，完成请求后不需要返回地面一层，

输入
每个输入文件包含一个测试用例。
每个用例包含一个正整数N，接下来是N个正数。
输入里的所有数字小于100.

输出
对于每个测试用例，在单个行输出总时间。
